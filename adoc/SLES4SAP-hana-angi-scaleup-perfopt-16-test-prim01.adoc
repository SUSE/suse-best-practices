.Test {testname}
==========
.{testComp}
 - {testcomponent}

.{testDescr}
- {testdescription}

.{testProc}
. {testprocedure}
+
[subs="attributes,quotes"]
----
{testcommand}
----

.{testRecover}
. Cleanup the sbd device (or devices) - to be called on any cluster node
+
[subs="attributes,quotes"]
----
# cs_clear_sbd_devices --all
----
+
. Start the cluster on failed node - to be called on any cluster node
+
[subs="attributes,quotes"]
----
# crm cluster start --all
----

.{testExpect}
.  The cluster detects the stopped primary {HANA} database (on {nodefail})
and marks the resource failed.
.  The {nodefail} gets fenced.
.  The cluster promotes the secondary {HANA} database (on {noderemain}) to
take over as primary.
.  The cluster migrates the IP address to the new primary (on {noderemain}).
.  The failed {nodefail} does not join the cluster - *admin to process the recovery procedure*
.  Once {nodefail} joins back to the cluster, {HANA} is registered as new secondary and started by the cluster.
.  After the some time the new secondary is marked as in sync (SOK).
.  The cluster "failed actions" are cleaned up after following the
recovery procedure.
==========

