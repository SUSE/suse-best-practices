<?xml version="1.0" encoding="UTF-8"?>
<!--<?oxygen RNGSchema="http://www.oasis-open.org/docbook/xml/5.0/rng/docbook.rng" type="xml"?>-->
<!DOCTYPE article [
<!ENTITY % entity SYSTEM "entity-decl.ent">
%entity;
]>

<!-- I use special character &#8288; to avoid line brakes after the leading hyphen of compiler
     options (such as -O2 or -ffast-math).  It looks horrible in the document source but fixes
     the issue, at least in the pdf output. -->


<article role="sbp" xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xmlns:its="http://www.w3.org/2005/11/its"
 xml:id="art-sbp-gcc15-sle16" xml:lang="en">

 <info>
  <title>Advanced Optimization and New Capabilities of GCC 15</title>

  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker>
    <dm:url>https://github.com/SUSE/suse-best-practices/issues/new</dm:url>
    <dm:product>Advanced Optimization and New Capabilities of GCC 15</dm:product>
   </dm:bugtracker>
   <dm:editurl>https://github.com/SUSE/suse-best-practices/blob/master/xml/</dm:editurl>
  </dm:docmanager>

  <meta name="series" its:translate="no">Best Practices</meta>
  <meta name="category" its:translate="no">
   <phrase>Development Tools</phrase>
  </meta>
  <meta name="task" its:translate="no">
   <phrase>Configuration</phrase>
  </meta>
  <meta name="title" its:translate="yes">Advanced Optimization and New Capabilities of GCC 15</meta>
  <meta name="description" its:translate="yes">Overview of GCC 15 and compilation optimization
   options for applications</meta>
  <meta name="social-descr" its:translate="yes">Advanced optimization and new capabilities of GCC
   15</meta>
  <meta name="productname" its:translate="no">
   <productname version="16.0">SUSE Linux Enterprise Server</productname>
  </meta>

  <meta name="platform" its:translate="no">SUSE Linux Enterprise Server 16.0 and later</meta>

  <authorgroup>
   <author>
    <personname>
     <firstname>Martin</firstname>
     <surname>Jambor</surname>
    </personname>
    <affiliation>
     <jobtitle>Toolchain Team Lead</jobtitle>
     <orgname>SUSE</orgname>
    </affiliation>
   </author>

   <author>
    <personname>
     <firstname>Jan</firstname>
     <surname>Hubička</surname>
    </personname>
    <affiliation>
     <jobtitle>Toolchain Developer</jobtitle>
     <orgname>SUSE</orgname>
    </affiliation>
   </author>

   <author>
    <personname>
     <firstname>Richard</firstname>
     <surname>Biener</surname>
    </personname>
    <affiliation>
     <jobtitle>Toolchain Developer</jobtitle>
     <orgname>SUSE</orgname>
    </affiliation>
   </author>

   <author>
    <personname>
     <firstname>Michael</firstname>
     <surname>Matz</surname>
    </personname>
    <affiliation>
     <jobtitle>Toolchain Developer</jobtitle>
     <orgname>SUSE</orgname>
    </affiliation>
   </author>

   <author>
    <personname>
     <firstname>Venkataramanan</firstname>
     <surname>Kumar</surname>
    </personname>
    <affiliation>
     <jobtitle>PMTS Software System Design Eng</jobtitle>
     <orgname>AMD</orgname>
    </affiliation>
   </author>

   <author>
    <personname>
     <firstname>Kim</firstname>
     <surname>Naru</surname>
    </personname>
    <affiliation>
     <jobtitle>Engineering Manager</jobtitle>
     <orgname>AMD</orgname>
    </affiliation>
   </author>

   <!--   <editor>
   <orgname></orgname>
   </editor>
   <othercredit>
   <orgname></orgname>
   </othercredit>-->
  </authorgroup>

  <cover role="logos">
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="suse.svg" width="5em" align="center" valign="bottom"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="suse.svg" width="152px" align="center" valign="bottom"/>
    </imageobject>
    <textobject>
     <phrase>SUSE logo</phrase>
    </textobject>
   </mediaobject>
  </cover>

  <revhistory xml:id="rh-art-sbp-gcc15-sle16">
   <revision>
    <date>2026-08-15</date>
    <revdescription>
     <para>Firt published version </para>
    </revdescription>
   </revision>
  </revhistory>


  <abstract>
   <para> The document at hands provides an overview of GCC 15 as the default compiler for users of
    SUSE Linux Enterprise 16.0. It focuses on the important optimization levels and options
     <emphasis role="strong">Link Time Optimization (LTO)</emphasis> and <emphasis role="strong"
     >Profile Guided Optimization (PGO)</emphasis>. Their effects are demonstrated by compiling the
    SPEC CPU benchmark suite for AMD EPYC 8635P Processor.</para>

   <!-- If we manage to revive FF analysis put the following back: ...and building Mozilla Firefox
         for a generic <literal>x86_64</literal> machine. -->
   <para>
    <emphasis role="strong">Disclaimer: </emphasis> Documents published as part of the SUSE Best
    Practices series have been contributed voluntarily by SUSE employees and third parties. They are
    meant to serve as examples of how particular actions can be performed. They have been compiled
    with utmost attention to detail. However, this does not guarantee complete accuracy. SUSE cannot
    verify that actions described in these documents do what is claimed or whether actions described
    have unintended consequences. SUSE LLC, its affiliates, the authors, and the translators may not
    be held liable for possible errors or the consequences thereof. </para>


  </abstract>
 </info>

 <sect1 xml:id="sec-gcc15-overview">
  <title>Overview</title>

  <para> The first release of the GNU Compiler Collection (GCC) with the major version 15, GCC 15.1,
   took place in May 2025. In June of the same year, the openSUSE Tumbleweed Linux distribution
   began using this compiler to build its packages. GCC 15.2, with fixes to over 100 bugs, was
   released in August. When SUSE Linux Enterprise Server 16.0 was released in November, this
   compiler was provided for building user applications and libraries. This new version introduces
   many new features. These include implementation of parts of the most recent versions of various
   language specifications (particularly C23, C++23, and C++26), along with their extensions (such
   as OpenMP and OpenACC). Like all new major releases, GCC 15 introduces support for new
   capabilities of a range of computer architectures and generic improvements in code optimization.
   Although GCC 15 is the third major release supporting AMD CPUs based on the Zen 5 core, both
   general and targeted optimizations allow it to better leverage processor capabilities.</para>

  <para> This document provides an overview of GCC 15. It focuses on selecting appropriate
   optimization options for your application and stresses the benefits of advanced modes of
   compilation. First, we describe the optimization levels the compiler offers, and other important
   options developers often use. We explain when and how you can benefit from using <emphasis
    role="bold">Link Time Optimization (LTO)</emphasis> and <emphasis role="bold">Profile Guided
    Optimization (PGO)</emphasis> builds. We also detail their effects when building a set of
   well-known CPU intensive benchmarks. Finally, we look at how these perform on AMD Zen 5 based AMD
   EPYC 8635P Processors. </para>
  <!-- If we manage to revive FF analysis put the following back: Finally, we take a closer look at
       the effects they have on a big software project: Mozilla Firefox. -->
 </sect1>

 <!-- The important box below (and other elements) just look bad in pdf without this.  -->
 <?pdfpagebreak style="sbp" formatter="fop"?>

 <sect1 xml:id="sec-gcc15-sle16-compilers">
  <title>Compilers available in SUSE Linux Enterprise Server 16</title>

  <para> Developers of user-space applications and libraries can use the supported GNU Compiler
   Collection (GCC) C, C++ and Fortran compilers. Compilers for other languages, cross-compilers and
   accelerator offloading compilers are not available from standard repositories, but developers can
   install them from Package Hub with community support. </para>

  <screen>sles16:~ # gcc --version
gcc (SUSE Linux) 15.2.0
Copyright (C) 2025 Free Software Foundation, Inc.
This is free software; see the source for copying conditions.  There is NO
warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
</screen>

  <para> Unlike the previous version, SUSE Linux Enterprise Server 16 has a different lifecycle for
   a number of components including compilers. The initial compiler major version in versions 16.0
   and 16.1 is GCC 15. Later SUSE Linux Enterprise Server releases will introduce the tick-tock
   model: </para>
  <itemizedlist>
   <listitem>
    <para>Each future even-numbered minor release of SUSE Linux Enterprise Server introduces a new
     major GCC version as the default compiler. This version is supported throughout long-term
     support (LTS) for that minor release and the subsequent minor release. It will also be
     supported also for the next SUSE Linux Enterprise Server minor version. For example, GCC 17 is
     introduced in SUSE Linux Enterprise Server 16.2. It is supported until the end of LTS for both
     SUSE Linux Enterprise Server 16.2 and 16.3.</para>
   </listitem>
   <listitem>
    <para> Each future odd-numbered minor release of SUSE Linux Enterprise Server (the tock release)
     introduces a new non-default major version of GCC. This version will not be the default one. To
     use this version, you need to explicitly invoke the binaries <literal>gcc-x</literal>,
      <literal>g++-x</literal> and <literal>gfortran-x</literal>. These non-default versions are
     supported for 24 months. </para>
   </listitem>
  </itemizedlist>

  <note>
   <title>Comparison with SUSE Linux Enterprise Server 15</title>
   <para> In SUSE Linux Enterprise Server 15, the one default <emphasis role="italic">system
     compiler</emphasis> was GCC 7 across the entire lifecycle and all service packs. Additionally,
    an <emphasis role="italic">application compiler</emphasis> was also available through the
     <emphasis role="italic">Development Tools Module</emphasis>, but it was never the default and
    its major version updated annually. The new scheme in SUSE Linux Enterprise Server 16 offers
    developers on this platform access to a more recent default compiler supported throughout the
    lifetime of each corresponding minor versions. Compilers added in odd-numbered minor releases
    enable developers to use the latest features and hardware support. </para>
  </note>

  <para> The basic C and C++ run time libraries are upgraded to newer GCC major versions across all
   SUSE Linux Enterprise 16 minor versions. Therefore, applications built with non-default "tock"
   compilers can run on fully-upgraded systems without requiring the non-default compiler. </para>

  <para> In this document, GCC 15 refers to any minor version within major version 15, whereas GCC
   15.2 refers specifically to that particular version. In most cases, these terms are
   interchangeable. </para>

  <sect2 xml:id="sec-gcc15-kernel-build-compiler">
   <title>Compiler for kernel modules and package rebuilds</title>

   <para> The operating system kernel of SUSE Linux Enterprise Server 16.0 was built using GCC 13.
    To build a kernel module for it, you should use the corresponding compiler from package
     <literal>gcc13</literal>. Nevertheless, building kernel modules is outside of the scope of this
    article. For more information please refer to the <link
     xlink:href="https://documentation.suse.com/sbp/systems-management/html/KMP-Manual-SLES16/index.html"
     >Kernel Module Package Manual for SUSE Linux Enterprise Server 16.0</link>. </para>

   <para> Most, but not all, other packages for SUSE Linux Enterprise are built using GCC 13. To
    perform a build in the same fashion as SUSE did when producing a particular package, use the C
    compiler referenced above or the corresponding packages for other languages from Package Hub.
    The correct compiler version is selected automatically when using the Open Build Service to
    build a package. However, this version might not be GCC 15. </para>

   <important>
    <title>Support status of GCC 13</title>
    <para> Only the C compiler from GCC 13 is supported, and only to build kernel module packages in
     accordance with SUSE policies. Other compilers from GCC 13 receive <emphasis role="italic"
      >community support basis through Package Hub</emphasis>. Using the C compiler to build
     user-space applications or libraries is only covered with <emphasis role="italic">commercially
      reasonable support</emphasis>. </para>
   </important>
  </sect2>

  <sect2 xml:id="sec-gcc15-comparison-against-older">
   <title>New features of GCC 15 compared to previous releases</title>

   <para> Each major version of GCC introduces new features, new language versions, support for the
    most modern processors, and performance and other improvements. GCC 15 is the first compiler to
    support the C23 standard, published in 2024 as ISO/IEC 9899:2024. This lets you take advantage
    of all included new language features. This standard, with GNU extensions, is now also the
    default when no standard is specified on the command line.</para>

   <para> The default, and latest fully implemented, C++ standard is ISO/IEC 14882:2017, commonly
    referred to as C++17. This version, with GNU extensions, is also the default when no standard is
    specified on the compiler command line. The compiler implements almost all features from C++20
    (ISO/IEC 14882:2020), C++23 (ISO/IEC 14882:2024), and most features expected in C++26. However,
    consider these features experimental. Use them with appropriate caution and avoid linking code
    produced by different compilers. Note that C++ <emphasis role="italic">modules</emphasis> are
    only partially implemented <footnote>
     <para> Proposals P1766R1 and P1815R2 are only implemented in GCC 16.</para>
    </footnote> and require the source file to be compiled with the
     <literal>-&#8288;fmodules-ts</literal> option. To check the implementation status of C++
    features in the compiler or the standard library, consult the following resources: </para>

   <itemizedlist>
    <listitem>
     <para>
      <link xlink:href="https://gcc.gnu.org/projects/cxx-status.html">C++ Standards Support in
       GCC</link>, and </para>
    </listitem>
    <listitem>
     <para>
      <link xlink:href="https://gcc.gnu.org/onlinedocs/gcc-15.2.0/libstdc++/manual">The GNU C++
       Library Manual</link>. </para>
    </listitem>
   </itemizedlist>

   <para>Advances in supporting new language specifications are not limited to C++. The Fortran
    compiler is also continuously improved. If you use <literal>OpenMP</literal> or
     <literal>OpenACC</literal> extensions for parallel programming, ythe compiler supports many
    features from newer versions of these standards. </para>

   <para>GCC 15 can generate code for many recent processors not supported by previous major
    versions. Such a list of processors is too long to include in this document. Nevertheless, <xref
     linkend="sec-gcc15-spec"/> describes code optimization for AMD EPYC 8635P Processors that are
    based on AMD Zen 5 cores. GCC supports this core architecture starting with versions 14.1 and
    13.2. Earlier versions do not support this core architecture and cannot optimize for it. </para>

   <para> Finally, the general optimization pipeline of the compiler has also significantly improved
    over time. For more details about improvements in versions of GCC 8 through 15, visit the links
    at the end of this section.</para>

  </sect2>

  <sect2 xml:id="sec-gcc15-issues-with-new-compiler">
   <title>Potential issues with the recent versions of the GCC compiler</title>

   <para> New versions of a compiler can sometimes behave differently and can introduce issues not
    present with the system compiler. The upstream project maintains extensive lists of such
    problems encountered by users which are referenced in the following section. This section
    highlights the most common pitfalls.</para>

   <para> Starting with GCC 14, the C compiler actually issues errors constructs disallowed since
    the 1999 ISO C standard. GCC 13 and earlier versions generated only warnings for these
    constructs:</para>
   <itemizedlist>
    <listitem>
     <para> Implicit int types (<literal>-Werror=implicit-int</literal>) </para>
    </listitem>
    <listitem>
     <para> Implicit function declarations
       (<literal>-&#8288;Werror=implicit-function-declaration</literal>), </para>
    </listitem>
    <listitem>
     <para> Wrong or misspelled function prototypes
       (<literal>-&#8288;Werror=declaration-missing-parameter-type</literal>), </para>
    </listitem>
    <listitem>
     <para> Incorrect uses of the return statement
       (<literal>-&#8288;Werror=return-mismatch</literal>), </para>
    </listitem>
    <listitem>
     <para> Using pointers as integers and vice versa
       (<literal>-&#8288;Werror=int-conversion</literal>), and </para>
    </listitem>
    <listitem>
     <para> Type mismatches of pointer types
       (<literal>-&#8288;Werror=incompatible-pointer-types</literal>) </para>
    </listitem>
   </itemizedlist>

   <para> We highly recommend fixing any of the above issues if you encounter them in your code.
    They are a frequent source of bugs, portability and even security issues. For more information
    and common solutions, see the <quote>Porting to GCC 14</quote> document referenced below. For
    code written in a C standard prior to C99 (1999 ISO standard), use the
     <literal>-std=gnu89</literal> or <literal>-std=c89</literal> option to allow these constructs.
    If your code uses features of the C99 standard or a later one, and for some reason cannto be
    fixed, convert specific errors to warnings using the <literal>-&#8288;Wno-error=</literal>
    option. Alternatively, use a new compiler switch <literal>-&#8288;fpermissive</literal> to
    apply this behavior to all new errors.</para>

   <note>
    <title>Impact on build environment probing</title>
    <para> Many code snippets (also called <emphasis role="italic">probes</emphasis>) generated by
      <literal>autoconf</literal> to discover the availability of various features trigger a compile
     error when a feature is missing. New compiler errors can cause build failures in code that
     previously compiled. This can result in available features being silently disabled (despite the
     fact they are actually available). <literal>autoconf</literal> has supported C99 compilers
     since version 2.69 in its generic, core probes. However, earlier versions or very specific
     probes might rely on C features removed in C99 and thus fail with GCC 14 or newer. To verify
     there are no unexpected differences, compare generated files such as
      <filename>config.log</filename>, <filename>config.h</filename> and other generated files using
      <command>diff</command>. </para>
   </note>

   <para>In addition, the C compiler in GCC 15 changes the default language version for C
    compilation from <literal>-std=gnu17</literal> to <literal>-std=gnu23</literal> which can lead
    to the following new compilation errors:</para>

   <itemizedlist>
    <listitem>
     <para>There are new keywords including <literal>bool</literal>, <literal>true</literal>,
       <literal>false</literal>, <literal>nullptr</literal>, and <literal>thread_local</literal>.
      Code that uses these for identifiers is no longer accepted.</para>
    </listitem>
    <listitem>
     <para>Function declarations without parameters have changed meaning in C23. In earlier
      standards, an empty parameter list conveyed no parameter information. in C23, it explicitly
      indicates that the function takes no parameters. This change also affects function pointer
      type definitions, which can lead to type mismatches.</para>
    </listitem>
   </itemizedlist>

   <para>Again, we recommend porting the code to the new standard. Alternatively, you can use the
     <literal>-std=</literal> option to select an older version of the standard to restore the old
    behavior.</para>


   <para>Users of the C compiler switching from GCC 9 or earlier should also be aware that newer
    versions default to <literal>-&#8288;fno-common</literal> for performance reasons. This
    means a linker error will now be reported if the same variable is defined in two C compilation
    units. This can occur if two or more <filename>.c</filename> files include the same header file.
    If the header declares a variable without the <literal>extern</literal> keyword when doing so,
    this inadvertently results in the creation of multiple definitions. If you encounter such an
    error, you need to add the <literal>extern</literal> keyword to the declaration in the header
    file and define the variable in only a single compilation unit. Alternatively, you can compile
    your project with an explicit <literal>-&#8288;fcommon</literal> if you are willing to
    accept that this behavior is inconsistent with C++ and may incur speed and code size penalties. </para>

   <para> Users compiling C++ sources should take note that <literal>g++</literal> version 11 and
    later default to <literal>-std=gnu++17</literal>, the C++17 standard with GNU extensions. This
    means that dynamic exception specifications are no longer allowed. Users can just annotate
    functions with <literal>noexcept</literal> as appropriate. Moreover, some C++ Standard Library
    headers no longer include headers they do not depend on. You may need to explicitly include
     <literal>&lt;limits&gt;</literal>, <literal>&lt;memory&gt;</literal>,
     <literal>&lt;utility&gt;</literal> or <literal>&lt;thread&gt;</literal>.</para>

   <para> The final issue emphasized here is that the C++ compiler in GCC 8 and later now assumes
    that no execution path in a non-void function reaches the end of the function without a return
    statement. This means it is assumed that such code paths will never be executed, and thus they
    will be eliminated. You should therefore pay special attention to warnings produced by
     <literal>-Wreturn-type</literal>. This option is enabled by default and indicates which
    functions are affected. </para>
  </sect2>

  <sect2 xml:id="sec-gcc15-more-info-on-changes">
   <title>References to more information on changes in recent GCC versions</title>

   <para>This section provides a comprehensible list of links to the description of new features and
    possible issues with each major version of GCC from 8 to 15 for your reference.</para>

   <para>
    <itemizedlist>
     <listitem>
      <para>GCC 8</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-8/changes.html">GCC 8 Release Series Changes, New
          Features, and Fixes</link>, </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-8/porting_to.html">Porting to GCC 8</link>,
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 9</para>

      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-9/changes.html">GCC 9 Release Series Changes, New
          Features, and Fixes</link>, </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-9/porting_to.html">Porting to GCC 9</link>,
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 10</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-10/changes.html">GCC 10 Release Series Changes,
          New Features, and Fixes</link>, </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-10/porting_to.html">Porting to GCC 10</link>,
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 11</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-11/changes.html">GCC 11 Release Series Changes,
          New Features, and Fixes</link>, </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-11/porting_to.html">Porting to GCC 11</link>,
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 12</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-12/changes.html">GCC 12 Release Series Changes,
          New Features, and Fixes</link>, </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-12/porting_to.html">Porting to GCC 12</link>,
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 13</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-13/changes.html">GCC 13 Release Series Changes,
          New Features, and Fixes</link>, and </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-13/porting_to.html">Porting to GCC 13</link>, and
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 14</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-14/changes.html">GCC 14 Release Series Changes,
          New Features, and Fixes</link>. </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-14/porting_to.html">Porting to GCC 14</link>.
        </para>
       </listitem>
      </itemizedlist>
     </listitem>

     <listitem>
      <para>GCC 15</para>
      <itemizedlist>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-15/changes.html">GCC 15 Release Series Changes,
          New Features, and Fixes</link>. </para>
       </listitem>
       <listitem>
        <para>
         <link xlink:href="https://gcc.gnu.org/gcc-15/porting_to.html">Porting to GCC 15</link>.
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </itemizedlist>

   </para>
  </sect2>

 </sect1>

 <sect1 xml:id="sec-gcc15-optimization-levels">
  <title>Optimization levels and related options</title>

  <para> GCC features an optimization pipeline controlled by approximately a hundred of command line
   options. Forcing users to decide whether to enable each option during compilation is impractical.
   Like all other modern compilers, GCC therefore introduces the concept of optimization levels:
   predefined optimization levels let you select from common configurations. Optionally, you can
   fine-tune individual options within a selected level if needed.</para>

  <para> The default is to not optimize. You can specify this optimization level on the command line
   as <literal>-&#8288;O0</literal>. It is often used when developing and debugging a project.
   This is usually accompanied by the command line switch <literal>-g</literal> so that debug
   information is emitted. Because no optimizations occur, no information is lost. Variables are not
   optimized away, and the compiler inlines only functions with attributes that explicitly require
   it. As a result, the debugger can reliably find everything it searches for in the running program
   and report the program state. However, the resulting code is large and slow. Thus, do not use
   this optimization level for release builds. </para>

  <para> The most common optimization level for release builds is <literal>-&#8288;O2</literal>.
   It attempts to optimize the code aggressively but avoids large compile times and excessive code
   growth. Optimization level <literal>-&#8288;O3</literal> instructs GCC to optimize as much as
   possible. This might increase code size and compilation time. Note that neither
    <literal>-&#8288;O2</literal> nor <literal>-&#8288;O3</literal> affects the precision
   and semantics of floating-point operations. Even at the optimization level
    <literal>-&#8288;O3</literal>, GCC implements math operations and functions according to the
   respective IEEE and ISO rules <footnote>
    <para> When the rounding mode is set to the default round-to-nearest (look up
      <literal>-&#8288;frounding-&#8288;math</literal> in the manual).</para>
   </footnote>. However, it allows floating-point expression contraction, for example when fusing an
   addition and a multiplication into one operation<footnote>
    <para>See documentation of <literal>-&#8288;ffp-&#8288;contract.</literal></para>
   </footnote>. This often means that the compiled programs run markedly slower than necessary if
   such strict adherence is not required. The <literal>-&#8288;ffast-math</literal> command-line
   option is a common way to relax rules governing floating-point operations. Listing all options
   enabled by <literal>-&#8288;ffast-math</literal> is outside the scope of this document.
   However, if performance is a priority for your floating-point calculations, review the available
   options in the GCC manual.</para>

  <para> If your project and the techniques you use to debug or instrument it do not depend on
    <emphasis role="italic">ELF symbol interposition</emphasis>, consider using
    <literal>-&#8288;fno-semantic-interposition</literal> to improve performance. This option
   allows the compiler to inline calls and propagate information even when it would be illegal if a
   symbol changed during dynamic linking. Using this option to signal to the compiler that
   interposition will not occur significantly improves performance of some projects, including the
   Python interpreter. </para>

  <para> The most aggressive optimization level is <literal>-&#8288;Ofast</literal> which
   implies <literal>-&#8288;ffast-math</literal> and
    <literal>-&#8288;fno-semantic-interposition</literal> along with a few other options that
   disregard strict standard compliance. In GCC 15, this level also means optimizers may introduce
   data races when moving memory stores, which may not be safe for multithreaded applications.
   Additionally, the Fortran compiler can take advantage of associativity of math operations even
   across parentheses and convert big memory allocations on the heap to allocations on stack. The
   last mentioned transformation may cause the code to violate maximum stack size allowed by
    <command>ulimit</command> which is then reported to the user as a segmentation fault. To work
   around this issue, use <command>ulimit -S</command> with a sufficiently high limit, or
    <command>ulimit -S unlimited</command>. We often use level <literal>-&#8288;Ofast</literal>
   to build benchmarks. It serves as a shorthand for options beyond
    <literal>-&#8288;O3</literal> that often make benchmarks run faster. Most benchmarks are
   intentionally designed to run correctly even when these strict standard compliance rules are
   relaxed. While this level may not be safe for general use, you can review the enabled options and
   evaluate each individually to optimize performance.</para>

  <para> If you feed the compiler with large machine-generated input, especially if individual
   functions are extremely large, compile time can become an issue even when using
    <literal>-&#8288;O2</literal>. In such cases, use optimization level
    <literal>-&#8288;O1</literal> to avoid running almost all optimizations with quadratic
   complexity. Finally, the <literal>-&#8288;Os</literal> level directs the compiler to
   aggressively optimize for the size of the binary. </para>

  <note>
   <title>Optimization level recommendation</title>
   <para> We usually recommend using <literal>-&#8288;O2</literal>. This is the optimization
    level we use to build most SUSE and openSUSE packages. At this level, the compiler makes
    balanced size and speed trade-offs when building a general-purpose operating system. However, we
    suggest using <literal>-&#8288;O3</literal> if your project is compute-intensive and either
    small or an important part of your actual workload. Moreover, if the compiled code contains
    performance-critical floating-point operations, investigate whether
     <literal>-&#8288;ffast-math</literal> or any of the fine-grained options it implies can be
    safely used. </para>
  </note>

  <para> Some projects use <literal>-&#8288;fno-strict-aliasing</literal> to work around
   type-punning problems in the source code. This is not recommended except for very low-level,
   hand-optimized code such as the Linux kernel. Type-based alias analysis is a powerful tool. It
   enables transformations like store-to-load propagation that in turn enable other high-level
   optimizations, including aggressive inlining and vectorization. </para>

  <para> With the <literal>-g</literal> option, GCC generates useful debug information even when
   optimizing. However, much information is irrecoverably lost in the process. Debuggers also
   struggle to present a view of the state of a program in which statements are not necessarily
   executed in the original order. Debugging optimized code can therefore be difficult, but is
   usually still possible. </para>

  <para>The compiler manual provides the complete list of optimization and command-line options. The
   manual is available in info format in the <package>gcc-info</package> package or online at <link
    xlink:href="https://gcc.gnu.org/onlinedocs/gcc-15.2.0/gcc/">the GCC project Web site</link>. </para>

  <para>Keep in mind that, while nearly all optimizing compilers have optimization levels sharing
   names for example with GCC, they do not necessarily involve the same trade-offs. For example,
   GCC's <literal>-&#8288;Os</literal> optimizes for size much more aggressively than
   LLVM/Clang's level of the same name. Therefore, it often produces slower code. The more
   equivalent option in Clang is <literal>-&#8288;Oz</literal>. Similarly,
    <literal>-&#8288;O2</literal> can have different meanings for different compilers. For
   example, the difference between <literal>-&#8288;O2</literal> and
    <literal>-&#8288;O3</literal> is greater in GCC than in LLVM/Clang. </para>

  <note>
   <title>Changing the optimization level with <command>cmake</command></title>
   <para> If you use <command>cmake</command> to configure and set up builds of your application, be
    aware that its <emphasis role="italic">release</emphasis> optimization level defaults to
     <literal>-&#8288;O3</literal>. This default might not be what you want. To change it,
    modify the <literal>CMAKE_C_FLAGS_RELEASE</literal>, <literal>CMAKE_CXX_FLAGS_RELEASE</literal>
    and/or <literal>CMAKE_Fortran_FLAGS_RELEASE</literal> variables. Because these variables are
    appended at the end of compilation command lines, they overwrite any level set in
     <literal>CMAKE_C_FLAGS</literal>, <literal>CMAKE_CXX_FLAGS</literal> and similar variables.
   </para>
  </note>
 </sect1>

 <sect1 xml:id="sec-gcc15-target-options">
  <title>Taking advantage of newer processors</title>

  <para> By default, GCC assumes that you want to run the compiled program on a wide variety of
   CPUs, including fairly old ones, regardless of the selected optimization level. Specifically when
   using GCC 15 on SUSE Linux Enterprise Server 16.0 and building applications for the
    <literal>x86_64</literal> architecture, the compiler will by default target CPUs capable of
   running architecture level <literal>X86-64-v2</literal><footnote>
    <para>These are CPUs that support features <literal>CMPXCHG16B (CX16)</literal>,
      <literal>LAHF-SAHF</literal>, <literal>POPCNT</literal>, <literal>SSE3</literal>,
      <literal>SSSE3</literal>, <literal>SSE4_1</literal>, and <literal>SSE4_2</literal>.</para>
   </footnote>. As one implication with a big potential performance impact, the compiler will not
   use <literal>AVX</literal> instructions, and let alone various <literal>AVX512</literal>
   instructions for floating-point and vector operations.</para>

  <para> If you know that the generated binary will only run on machines supporting newer
   instruction set extensions, specify it on the command line. A complete list is available in the
   manual, but the most prominent option <literal>-&#8288;march</literal> lets you select a CPU
   model to generate code for. For example, if your program will only be executed on AMD EPYC 8635P
   Processors based on AMD Zen 5 cores or compatible hardware, instruct GCC to take advantage of all
   the instructions the CPU supports with the option
   <literal>-&#8288;march=znver5</literal>.</para>

  <para> To run the program on the machine on which you compile it, let the compiler auto-detect the
   target CPU model with the <literal>-&#8288;march=native</literal> option. This works reliably
   only if the compiler is new enough to know the target CPU. Otherwise, that is not the case, the
   compiler falls back to the vendor's last known generation (that is, for example,
    <literal>znver3</literal> instead of <literal>znver5</literal>.</para>

  <para>Consider using <literal>-&#8288;march=x86-64-v3</literal> to tell the compiler that the
   CPUs executing the program are required to support all features defined for architecture level
    <literal>X86-64-v3</literal>. These include all AMD CPUs based on a Zen core, starting with
   Zen1. The most important of these features is probably <literal>AVX2</literal>, which allows the
   compiler to use 256bit vector instructions to significantly boost performance when
   supported.</para>

 </sect1>

 <sect1 xml:id="sec-gcc15-lto">
  <title>Link Time Optimization (LTO)</title>

  <para>
   <xref linkend="fig-gcc15-nonlto-build" xrefstyle="template:Figure %n"/> outlines the classic mode
   of operation of a compiler and a linker. Pieces of a program are compiled and optimized in chunks
   defined by the user called compilation units to produce so-called object files. These object
   files already contain binary machine instructions and are combined together by a linker. Because
   the linker works at a low level, it cannot perform much optimization. Thus, deviding the program
   into compilation units presents a profound barrier to optimization. </para>

  <figure xml:id="fig-gcc15-nonlto-build">
   <title>Traditional program build</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="gcc12-nonlto.svg" width="100%" format="SVG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="gcc12-nonlto.svg" width="100%" format="SVG"/>
    </imageobject>
   </mediaobject>
  </figure>

  <para> To overcome this limitation, rearrange the process so that the linker does not receive
   nearly finished object files with machine instructions as input, but rather is invoked on files
   containing so-called <emphasis role="italic">intermediate language</emphasis> (IL). This format
   provides a much richer representation of each original compilation unit (see <xref
    linkend="fig-gcc15-lto-build" xrefstyle="template:figure %n"/>). The linker identifies the input
   as incomplete and invokes a linker plugin that runs the compiler again. This time, the compiler
   has access to the representation of the entire program or library being built. The compiler
   decides which optimizations ato perform across function and compilation unit boundaries, and then
   divides the program into a set of partitions. Each partition is then optimized independently,
   machine code is emitted for it, and the result is finally linked the traditional way. Processing
   of the partitions is performed in parallel. </para>

  <figure xml:id="fig-gcc15-lto-build">
   <title>Building a program with GCC using Link Time Optimization (LTO)</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="gcc12-lto.svg" width="100%" format="SVG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="gcc12-lto.svg" width="100%" format="SVG"/>
    </imageobject>
   </mediaobject>
  </figure>

  <para> To use <emphasis role="strong">Link Time Optimization</emphasis>, add the
    <literal>-&#8288;flto</literal> switch to the compilation command line. Most packages in
   SUSE Linux Enterprise Server 16.0 are built with LTO in this manner. LTO has been the default
   compilation mode for packages in the openSUSE Tumbleweed distribution for years. Much effort has
   gone into emitting quality debug information when building with LTO. Thus, the debugging
   experience is no longer severely limited as it when LTO was first introduced. </para>

  <para> LTO in GCC always consists of a <emphasis role="italic">whole program analysis</emphasis>
   (WPA) stage followed by the majority of the compilation process performed in parallel. This
   greatly reduces the build times of most projects. To control the parallelism, explicitly cap the
   number of parallel compilation processes by <emphasis role="italic">n</emphasis> if you specify
     <literal>-&#8288;flto=<replaceable>n</replaceable></literal> at linker command line.
   Alternatively, you can use the GNU <command>make</command> jobserver with
    <literal>-&#8288;flto=jobserv</literal> to instruct GNU make to keep the jobserver available
   to the linker process<footnote>
    <para>When using a <command>make</command> version earlier than 4.4, it was required to prepend
     the <emphasis role="strong">makefile</emphasis> rule invoking link step with character
      <literal>+</literal>. Fortunately, <command>make</command> in SUSE Linux Enterprise Server 16
     is new enough that this is no longer necessary.</para>
   </footnote>.</para>

  <para> You can also use <literal>-&#8288;flto=auto</literal> which instructs GCC to search for
   the jobserver. If it is not found, use all available CPU threads. </para>

  <para> Note that there is a principal architectural difference in how GCC and LLVM/Clang approach
   LTO. Clang provides two LTO mechanisms, so-called <emphasis role="italic">thin LTO</emphasis> and
    <emphasis role="italic">full LTO</emphasis>. In <emphasis role="italic">full LTO</emphasis>,
   LLVM processes the whole program as if it was a single translation unit that does not allow for
   any parallelism<footnote>
    <para> GCC can be configured to operate this way with the option
      <literal>-&#8288;flto-partition=one</literal>.</para>
   </footnote>. LLVM in <emphasis role="italic">thin LTO</emphasis> mode can compile different
   compilation units in parallel and enables inlining across compilation unit boundaries, but not
   most other cross-module optimizations. Therefore, this mechanism carries an inherently higher
   code quality penalty than <emphasis role="italic">full LTO</emphasis> or the GCC approach. </para>

  <sect2 xml:id="sec-gcc15-selected-lto-benefits">
   <title>Most notable benefits of LTO</title>

   <para> Applications built with LTO are often faster, mainly because the compiler can <emphasis
     role="italic">inline</emphasis> calls to functions in another compilation unit. This
    possibility also allows programmers to structure their code according to its logical division
    because they are not forced to put function definitions into header files to enable their
    inlining. As the compiler cannot inline all calls conveying information known at compilation
    time, GCC tracks and propagates constants, value ranges, memory reference information and
    devirtualization contexts from the call sites to the callees, even when passed in an aggregate
    or by reference. These can in turn save unnecessary computations or enable subsequent
    optimizations and speed up the built program or library. LTO allows such propagation across
    compilation unit boundaries, too. </para>

   <para> Link Time Optimization with <emphasis>whole program analysis</emphasis> also offers many
    opportunities to shrink the code size of the built project. Functions and their parts that are
    not necessary in any particular project can be removed with <emphasis role="italic">symbol
     promotion</emphasis> and inter-procedural <emphasis role="italic">unreachable code
     elimination</emphasis>. This occurs even when they are not declared <literal>static</literal>
    and are not defined in an anonymous namespace. Automatic <emphasis role="italic">attribute
     discovery</emphasis> identifies C++ functions that do not throw exceptions. This allows the
    compiler to avoid generating a lot of code in exception cleanup regions. <emphasis role="italic"
     >Identical code folding</emphasis> finds functions with the same semantics and removes all but
    one of them. The code size savings are often very significant and a compelling reason to use LTO
    even for applications which are not CPU-bound. </para>

   <note>
    <title>Building libraries with LTO</title>
    <para> The symbol promotion is controlled by resolution information given to the linker and
     depends on type of the DSO build. When producing a dynamically loaded shared library, all
     symbols with default visibility can be overwritten by the dynamic linker. This blocks the
     promotion of all functions not declared inline. Thus, it is necessary to use the hidden
     visibility wherever possible to achieve best results. To make hidden visibility the default,
     use the compiler flag <literal>-&#8288;fvisibility=hidden</literal>. Similar problems occur
     even when building static libraries with <literal>-rdynamic</literal>. </para>
   </note>
  </sect2>

  <sect2 xml:id="sec-gcc15-lto-issues">
   <title>Potential issues with LTO</title>

   <para> As mentioned earlier, the most packages in SUSE Linux Enterprise Server 16 and also in the
    openSUSE Tumbleweed community distribution are built with LTO by default and work well without
    tweaks. Nevertheless, some low-level constructs pose a problem for LTO. One typical issue
    involves symbols defined in <emphasis role="italic">inline assembly</emphasis>, which can end up
    in a different partition from their uses and subsequently fail final linking. To build such
    projects with LTO, place assembler snippets defining symbols into a separate assembler source
    file so they participate only in final linking. Global <literal>register</literal> variables are
    not supported by LTO, so programs must either avoid this feature or build the traditional way.
    You can also exclude specific compilation units from LTO by compiling them without
     <literal>-&#8288;flto</literal> or appending
     <literal>-&#8288;fno-&#8288;lto</literal> to the compilation command line). The rest of
    the program still benefits from using LTO.</para>

   <para> Another notable limitation of LTO is that it does not support <emphasis role="italic">
     symbol versioning</emphasis> implemented with special inline assembly snippets (as opposed to a
    linker map file). To define symbol versions in the source files, do so with the
     <literal>symver</literal> function attribute. As an example, the following snippet will make
    the function <literal>foo_v1</literal> implement <literal>foo</literal> in <emphasis
     role="italic">node</emphasis>
    <literal>VERS_1</literal> (which must be specified in the version script supplied to the
    linker). See <link
     xlink:href="https://gcc.gnu.org/onlinedocs/gcc/Common-Function-Attributes.html#index-symver-function-attribute"
     >the manual</link> for more details. </para>
   <screen>__attribute__ ((__symver__ ("foo@VERS_1")))
int foo_v1 (void)
{
}
      </screen>

   <para> Sometimes the extra power of LTO reveals pre-existing problems that otherwise remain
    hidden. Violations of (strict) <emphasis role="italic">aliasing</emphasis> rules and C++
     <emphasis role="italic">one definition rule</emphasis> tend to cause misbehavior significantly
    more often. The <literal>-Wodr</literal> warning reports the latter by default and should not be
    ignored. in addition, there are also cases where the use of the <literal>flatten</literal>
    function attribute can lead to excessive inlining with LTO. Furthermore, LTO is not suitable for
    code snippets compiled by <literal>configure</literal> scripts generated by
     <literal>autoconf</literal> to discover feature availability. This applies especially when the
    script searches for a string in the generated assembly. </para>

  </sect2>
 </sect1>

 <sect1 xml:id="sec-gcc15-pgo">
  <title>Profile-Guided Optimization (PGO)</title>

  <para> Optimizing compilers frequently make decisions based on which code path is most likely to
   execute, expected loop iterations, and similar estimates. They also often face trade-offs between
   potential runtime benefits and code size growth. Ideally, they optimize only frequently executed
   (so-called <emphasis role="italic">hot</emphasis>) parts of a program for speed and everything
   else for size. This reduces strain on caches and makes distribution of the built software
   cheaper. However, guessing which parts of a program are <emphasis role="italic">hot</emphasis> is
   difficult. Even sophisticated estimation algorithms implemented in GCC are no match for actual
   measurement. </para>

  <para> If you do not mind adding an extra level of complexity to the build system of your project,
   you can make such measurement part of the process. The <emphasis role="strong"
    >makefile</emphasis> (or any other) build script needs to compile the project twice. First,
   compile the code with the <literal>-&#8288;fprofile-generate</literal> option and execute the
   resulting binary in one or multiple <emphasis role="italic">train runs</emphasis>. During these
   runs, the program saves its behavior information to special files. Afterward, the project needs
   to be rebuilt again, this time with the <literal>-&#8288;fprofile-use</literal> option. This
   instructs the compiler to look for the files with the measurements and use them when making
   optimization decisions. This process is called <emphasis role="italic">Profile-Guided
    Optimization (PGO)</emphasis>. </para>

  <para> It is important that the train run exhibits the same characteristics as the real workload.
   Unless you use the option <literal>-&#8288;fprofile-partial-training</literal> in the second
   build, the train run must exercise the code most frequently executed in real use. Otherwise, it
   will be optimized for size and PGO will do more harm than good. With this option, GCC estimates
   properties of project portions not exercised in the train run, as if compiled without profile
   feedback. However, this also means that this code does not perform better or shrink as much as
   expected from a PGO build.</para>

  <para> On the other hand, train runs do not need to be a perfect simulation of the real workload.
   For example, even though a test suite should not be a very good train run in theory because it
   disproportionally often tests various corner cases, in practice many projects use it as a train
   run and achieve significant runtime improvements with real workloads, too. </para>

  <para> Profiles collected using an instrumented binary for multithreaded programs may be
   inconsistent because of missed counter updates. You can use
    <literal>-&#8288;fprofile-correction</literal> in addition to
    <literal>-&#8288;fprofile-use</literal> so that GCC uses heuristics to correct or smooth out
   such inconsistencies instead of emitting an error. </para>

  <para> Profile-Guided Optimization can be combined with and is complimentary to Link Time
   Optimization. While LTO expands what the compiler can do, PGO informs it which parts of the
   program are important and require focus. The case study in the following section shows how the
   two techniques work together on a well-known set of benchmarks. </para>
 </sect1>

 <sect1 xml:id="sec-gcc15-spec">
  <title>Performance evaluation: SPEC CPU 2017</title>

  <para>
   <emphasis role="italic">Standard Performance Evaluation Corporation</emphasis> (SPEC) is a
   non-profit corporation that publishes a variety of industry standard benchmarks to evaluate
   performance and other characteristics of computer systems. Its suite of CPU intensive workloads,
   called SPEC CPU 2017, is often used to compare compilers and how well they optimize code with
   different settings. This is because the included benchmarks are well known and represent a wide
   variety of computation-heavy programs. The following section highlights selected results of a GCC
   15 evaluation using the suite. </para>

  <para> Note that when we use SPEC to perform compiler comparisons, we are lenient toward some
   official SPEC rules that system manufacturers must observe for official scores. We disregard the
   concepts of <emphasis role="italic">base</emphasis> and <emphasis role="italic">peak</emphasis>
   metrics and focus on compilation results using a particular set of options. We also patched
   several benchmarks: </para>

  <itemizedlist>
   <listitem>
    <para> Benchmarks <literal>505.mcf_r</literal>, <literal>511.povray_r</literal>, and
      <literal>527.cam4_r</literal> contain an implementation of quicksort which violates (strict)
     C/C++ aliasing rules which can lead to erroneous behavior when optimizing at link time. SPEC
     decided not to change the released benchmarks and suggests that these benchmarks are built with
     the <literal>-&#8288;fno-strict-aliasing</literal> option when they are built with GCC.
     That makes evaluation of compilers using SPEC problematic, examining their ability to use
     aliasing rules to facilitate optimizations is important. We have therefore disabled it only for
     the problematic <literal>qsort</literal> functions with the following function attribute: </para>
    <screen>__attribute__((optimize("-fno-strict-aliasing")))</screen>
    <para> As a result, the only benchmark which we compile with
      <literal>-&#8288;fno-strict-aliasing</literal> is <literal>500.perlbench_r</literal> and
      <literal>502.gcc_r</literal><footnote>
      <para>The version of the GCC compiler from which <literal>502.gcc_r</literal> derived contains
        <link xlink:href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=48981">bug 48981</link> which
       was a strict aliasing rules violation. The upstream GCC project has fixed this issue so that
       it can be built with strict-aliasing enabled.</para>
     </footnote>. </para>
   </listitem>
   <listitem>
    <para> Benchmark <literal>511.povray_r</literal> cannot be built with option
      <literal>-&#8288;ffinite-math-only</literal> which is a part of options enabled by
      <literal>-&#8288;ffast-math</literal> for reasons described in <link
      xlink:href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=107021">GCC bug 107021</link>. The
      <literal>-&#8288;Ofast</literal> measurements using GCC 15 or LLVM 21 in this section
     therefore append <literal>-&#8288;fno-finite-math-only</literal> to the compilation command
     lines, but again only for this specific benchmark. </para>
   </listitem>
   <listitem>
    <para> We have increased the tolerance of <literal>549.fotonik3d_r</literal> to rounding errors
     after it became clear the intention was that the compiler can use relaxed semantics of
     floating-point operations in the benchmark (see <link
      xlink:href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=84201">GCC bug 84201</link>). </para>
   </listitem>
  </itemizedlist>

  <para> Moreover, SPEC 2017 CPU offers so-called <emphasis role="italic">speed</emphasis> and
    <emphasis role="italic">rate</emphasis> metrics. For our purposes, we mostly ignore the
   differences and run the benchmarks configured for rate metrics (mainly because the runtimes are
   smaller) but we always run all benchmarks single-threaded. For these and other reasons, all the
   results in this document are <emphasis role="italic"> non-reportable</emphasis>. Nevertheless,
   all presented results of individual benchmarks are medians from three runs.</para>

  <para> Finally, SPEC specifies a base runtime for each benchmark and defines a <emphasis
    role="italic">rate</emphasis> as the ratio of the base runtime and the median measured runtime
   (this rate is a separate concept from the rate metrics). The overall suite score is then
   calculated as geometric mean of these ratios. The bigger the rate or score, the better it is. In
   the remainder of this section, we report runtimes using relative rates and their geometric means
   as they were measured on an AMD EPYC 8635P Processor running SUSE Linux Enterprise Server
   16.0.</para>

  <sect2 xml:id="sec-gcc15-spec-lto-pgo">
   <title>Benefits of LTO and PGO</title>

   <para> In <xref linkend="sec-gcc15-optimization-levels"/>, we recommend compiling HPC workloads
    with <literal>-&#8288;O3</literal> and benchmarks with <literal>-&#8288;Ofast</literal>.
    However, examining integer-crunching benchmarks built with only
     <literal>-&#8288;O2</literal> is still useful, as Linux distributions often build the
    underlying programs this way. As mentioned earlier, both SUSE and openSUSE build almost all
    recent distributions with LTO and selected packages with PGO. The following paragraphs
    demonstrate why. </para>

   <figure xml:id="fig-gcc15-specint-o2-pgolto-geomean">
    <title>Overall performance (bigger is better) of SPEC INTrate 2017 built with GCC 15.2 and
     -&#8288;O2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-o2-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-o2-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <!-- xrefstyle="select:label" in xref also works but puts Figure with capital F everywhere -->

   <para>
    <xref linkend="fig-gcc15-specint-o2-pgolto-geomean" xrefstyle="template:Figure %n"/> shows the
    overall performance effect on the whole integer benchmark suite as captured by the geometric
    mean of all individual benchmark rates. Using both PGO and LTO results in remarkable relative
    uplift of 15.6%. This occurs even though starting with GCC 12, the compiler can conservatively
    auto-vectorize code in <literal>525.x264_r</literal> at <literal>-&#8288;O2</literal>, which
    previously required PGO at this level. Nevertheless, this benchmark still benefits a lot when
    the profile information is available, together with several others derived from programs
    typically compiled with <literal>-&#8288;O2</literal>. This is illustrated in <xref
     linkend="fig-gcc15-specint-o2-pgolto-perf-indiv" xrefstyle="template:figure %n"/>. </para>

   <figure xml:id="fig-gcc15-specint-o2-pgolto-perf-indiv">
    <title>Runtime performance (bigger is better) of individual integer benchmarks built with GCC
     15.2 and -&#8288;O2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-o2-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-o2-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>
    <xref linkend="fig-gcc15-specint-o2-ltopgo-size" xrefstyle="template:Figure %n"/> shows another
    important advantage of LTO and PGO: a significant reduction of the size of the binaries
    (measured without debug info). Note that the figure does not show that the size of benchmark
     <literal>548.exchange2_r</literal> grew to 266% of the original size with PGO and to 177% with
    both PGO and LTO. Although this growth appears large, it stems from a small base. It is the only
    Fortran benchmark in the integer suite and, its size penalty is offset by significant speed-up,
    making the trade-off reasonable. For completeness, we show this result in <xref
     linkend="fig-gcc15-specint-o2-ltopgo-size-exchange" xrefstyle="template:figure %n"/>. When we
    sum sizes of all ten benchmarks, LTO shrinks size by 5%, PGO by 7.5% and using both by 14.5%. </para>

   <figure xml:id="fig-gcc15-specint-o2-ltopgo-size">
    <title>Binary size (smaller is better) of individual integer benchmarks built with GCC 15.2 and
     -&#8288;O2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-o2-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-o2-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specint-o2-ltopgo-size-exchange">
    <title>Binary size (smaller is better) of 548.exchange2_r built with GCC 15.2 and
     -&#8288;O2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-o2-pgolto-size-exchange.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-o2-pgolto-size-exchange.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para> Runtime benefits and binary size savings are also visible when using the optimization
    level <literal>-&#8288;Ofast</literal> and option
     <literal>-&#8288;march=native</literal>. This enables the compiler to take advantage of all
    instructions that the AMD EPYC 8635P Processor supports. <xref
     linkend="fig-gcc15-specint-ofast-pgolto-geomean" xrefstyle="template:Figure %n"/> shows the
    respective geometric means, and <xref linkend="fig-gcc15-specint-ofast-pgolto-perf-indiv"
     xrefstyle="template:figure %n"/> shows how rates change for individual benchmarks. Even at the
    aggressive optimization level, PGO brings clear benefits for benchmarks derived from
    interpreters and compilers like <literal>500.perlbench_r</literal> and
     <literal>502.gcc_r</literal>. However, the compiler can struggle to update measured profile
    information during complex inter-procedural optimizations, such as in
     <literal>548.exchange2_r</literal>. This can cause the technique to decrease performance.
    Finall, although optimization levels <literal>-&#8288;O3</literal> and
     <literal>-&#8288;Ofast</literal> permit to be relaxed about the final binary size, PGO and
    especially LTO can reduce binary size at these levels, too. <xref
     linkend="fig-gcc15-specint-ofast-pgolto-size" xrefstyle="template:Figure %n"/> shows the
    relative binary sizes of all integer benchmarks. </para>

   <figure xml:id="fig-gcc15-specint-ofast-pgolto-geomean">
    <title>Overall performance (bigger is better) of SPEC INTrate 2017 built with GCC 15.2 using
     -&#8288;Ofast and -&#8288;march=native</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specint-ofast-pgolto-perf-indiv">
    <title>Runtime performance (bigger is better) of individual integer benchmarks built with GCC
     15.2 using -&#8288;Ofast and -&#8288;march=native</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specint-ofast-pgolto-size">
    <title>Binary size (smaller is better) of SPEC INTrate 2017 built with GCC 15.2 using
     -&#8288;Ofast and -&#8288;march=native</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>Many of the SPEC 2017 floating-point benchmarks measure how well a system can optimize and
    execute a handful of number crunching loops. They often come from performance sensitive programs
    written with traditional compilation method in mind. As a result, there are fewer cross-module
    dependencies, making the identification of hot paths less critical. Consequently, the overall
    impact of LTO and PGO on the suite is often minimal. Nevertheless, there are important cases
    when these modes of compilation also bring significant performance increases. <xref
     linkend="fig-gcc15-specfp-ofast-pgolto-perf-indiv" xrefstyle="template:Figure %n"/> shows the
    effect of these methods on individual benchmarks when compiled at
     <literal>-&#8288;Ofast</literal> and targeting the full ISA of the AMD EPYC 8635P
    Processor. Furthermore, binary size savings of PGO and LTO are sometimes even bigger than those
    achieved on integer benchmarks, as can be seen in <xref
     linkend="fig-gcc15-specfp-ofast-pgolto-size" xrefstyle="template:figure %n"/></para>

   <para>Two cases require explanation here. First, <literal>519.lbm_r</literal> compiled with GCC
    15.2 and LTO suffers a code positioning problem. When the main hottest loop of the benchmark is
    placed at an unfortunate address, the CPU executes it considerably slower. This is rare and
    difficult to reproduce. Changing the start address of the encapsulating function is sufficient
    to mitigate the issue.</para>

   <para>Second, in the <literal>538.imagick_r</literal> benchmark, a mismatch exists between the
    code paths exercised in the train run and the reference run. The train run determines which
    program parts to optimize for speed, while the reference run provides the benchmark score. This
    is the problem we warn against in <xref linkend="sec-gcc15-pgo"/>, and it shows the predictable
    detrimental effect on performance.<footnote>
     <para>See <link xlink:href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=111551">GCC bug
       111551</link> for more details.</para>
    </footnote> Moreover, because the main loop, which is not appropriately optimized as not
    executed in the train run, is in a function in which another loop is heavily executed in the
    train run, even using the <literal>-&#8288;fprofile-partial-training</literal> does not help
    to mitigate the problem. This is a bug in the SPEC CPU suite that causes the overall performance
    score to decrease by 0.5% when using both LTO and PGO. </para>

   <!--
   <figure xml:id="fig-gcc12-specfp-ofast-pgolto-geomean">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 built with GCC 12.3 and
     -&#8288;Ofast</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc12-specfp-ofast-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc12-specfp-ofast-pgolto-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>
    -->

   <figure xml:id="fig-gcc15-specfp-ofast-pgolto-perf-indiv">
    <title>Runtime performance (bigger is better) of individual floating-point benchmarks built with
     GCC 15.2 using -&#8288;Ofast and -&#8288;march=native</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-pgolto-perf-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specfp-ofast-pgolto-size">
    <title>Binary size (smaller is better) of SPEC FPrate 2017 built with GCC 15.2 using
     -&#8288;Ofast and -&#8288;march=native</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-pgolto-size.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>



  </sect2>

  <sect2 xml:id="sec-gcc15-spec-cmp-to-gcc7">
   <title>GCC 15.2 compared to GCC 13</title>

   <para>Upgrading the compiler sometimes can lead to issues, as described in section <xref
     linkend="sec-gcc15-issues-with-new-compiler"/>. Therefore, users sometimes hesitate to upgrade
    when they do not need new features. Reasons to upgrade usually include better performance of
    code generated by new compilers. This chapter uses the SPEC CPU 2017 suite to examine how GCC 15
    compares to GCC13 in this regard. GCC 13 is only two years older and was the first major version
    targeting Zen5-based processors, such as the AMD EPYC 8635P Processor we used.</para>

   <figure xml:id="fig-gcc15-specint-ofast-vs13-geomean">
    <title>Overall performance (bigger is better) of SPEC INTrate 2017 built with GCC 13.4 and 15.2
     (-&#8288;Ofast -&#8288;march=native)</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vs13-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vs13-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>
    <xref linkend="fig-gcc15-specint-ofast-vs13-geomean" xrefstyle="template:Figure %n"/> captures
    the benefits of using the newer compiler with integer workloads in the form of relative
    improvements of the geometric mean across the SPEC INTrate 2017 suite. If we compare the
    geometric means obtained with LTO, GCC 15 achieves 5.9% better score. <xref
     linkend="fig-gcc15-specint-ofast-vs13-indiv" xrefstyle="template:Figure %n"/> dives deeper and
    shows which particular benchmarks gained most in terms of performance. The benchmark
     <literal>525.x264_r</literal> illustrates how newer compilers sometimes manage to use modern
    hardware better. Both GCC 13 and GCC 15 can correctly identify a Zen5-based CPU and can
    auto-vectorize code using 512 bit vectors. In this particular scenario, doing this alone, like
    GCC 13 does, reduces performance because the main loops do not iterate enough times. GCC 15
    produces cascaded vectorized epilogues in this scenario, ensuring the compiler uses the optimal
    vector width. Because big parts of compiler optimizations rely on heuristics, small regressions
    can still occur. In this scenario, without LTO, <literal>531.deepsjeng_r</literal> regressed by
    3%. However, compilation with LTO compilation showed no regression.</para>

   <figure xml:id="fig-gcc15-specint-ofast-vs13-indiv">
    <title>Runtime performance (bigger is better) of selected integer benchmarks built with GCC 13.4
     and 15.2 (-&#8288;Ofast -&#8288;march=native)</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vs13-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vs13-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>On the other hand, floating-point results show that performance rarely degrades severely.
    As previously discussed, <literal>519.lbm_r</literal> compiled with GCC 15.2 and LTO has a code
    positioning issue that slows execution significantly. Outside a benchmark context, this
    particular problem is easy to identify and fix by modifying the source code. For a standard
    benchmark, however, we can only report a 45% performance reduction of that particular benchmark.
    In contrast, <xref linkend="fig-gcc15-specfp-ofast-vs13-indiv" xrefstyle="template:figure %n"/>
    shows that the non-LTO variant of the benchmark improved slightly. This suggests the LTO variant
    would also improve if it avoided this issue. The same figure shows clear improvements in other
    benchmarks, particularly<literal>538.imagick_r</literal>. The compiler optimizes to avoid
     <emphasis role="italic">store-to-load forwarding stalls</emphasis>, yielding a 39% improvement
    in this benchmark.</para>

   <figure xml:id="fig-gcc15-specfp-ofast-vs13-indiv">
    <title>Runtime performance (bigger is better) of selected floating-point benchmarks built with
     GCC 13.4 and 15.2 (-&#8288;Ofast -&#8288;march=native)</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vs13-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vs13-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>The overall score for the floating point benchmarks is available in <xref
     linkend="fig-gcc15-specfp-ofast-vs13-geomean" xrefstyle="template:figure %n"/>. However, we
    also provide <xref linkend="fig-gcc15-specfp-ofast-vs13-geomean-excl-lbm"
     xrefstyle="template:figure
   %n"/> which compares the geometric mean of scores of all
    benchmarks except <literal>519.lbm_r</literal>. This provides a better comparison of the
    compilers because there is little that a compiler can do to avoid the code placement problem. in
    general, GCC 13 is as susceptible to this issue as the newer version.</para>


   <figure xml:id="fig-gcc15-specfp-ofast-vs13-geomean">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 built with GCC 13.4 and 15.2
     (-&#8288;Ofast -&#8288;march=native)</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vs13-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vs13-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specfp-ofast-vs13-geomean-excl-lbm">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 exluding
      <literal>519.lbm_r</literal> built with GCC 13.4 and 15.2 (-&#8288;Ofast
     -&#8288;march=native)</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vs13-geomean-excl-lbm.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vs13-geomean-excl-lbm.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>


  <sect2 xml:id="sec-gcc15-spec-fast-math">
   <title>Effects of <literal>-&#8288;ffast-math</literal> on floating-point performance</title>

   <para> In <xref linkend="sec-gcc15-optimization-levels"/>, we noted failing to relax
    floating-point math functions semantics can sacrifice performance. This applies even when strict
    adherence to all IEEE and/or ISO rules is unnecessary. This section uses the SPEC FPrate 2017
    test suite to demonstrate this performance impact. </para>

   <para> We have built the benchmarking suite using optimization level
     <literal>-&#8288;O3</literal>, LTO (though without PGO) and
     <literal>-&#8288;march=native</literal> to target the native ISA of the AMD EPYC 8635P
    Processor. Then we compared its runtime score against the suite built with these options and
     <literal>-&#8288;ffast-math</literal>. Overall, the build using
     <literal>-&#8288;ffast-math</literal> performed 7% better across all benchmarks. When
    excluding <literal>519.lbm_r</literal> from the comparison due to the code placement issue
    described earlier, performance imrpoved by 11%. Nevertheless, if you look at <xref
     linkend="fig-gcc15-specfp-o3-fastmath-indiv" xrefstyle="template:figure %n"/> it shows that the
    scores of five benchmarks improved by more than 10%, with <literal>538.imagick_r</literal>
    growing by 57%. </para>

   <!-- 
   <figure xml:id="fig-gcc14-specfp-o3-fastmath-geomean">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 built with GCC 14.2 and
     -&#8288;O3 -&#8288;flto -&#8288;march=native, without and with
     -&#8288;ffast-math</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc14-specfp-o3-fastmath-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc14-specfp-o3-fastmath-perf-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>
    -->

   <figure xml:id="fig-gcc15-specfp-o3-fastmath-indiv">
    <title>Runtime performance (bigger is better) of individual floating-point benchmarks built with
     GCC 15.2 and -&#8288;O3 -&#8288;flto -&#8288;march=native, without and with
     -&#8288;ffast-math</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-o3-fastmath-perf-indiv.svg" width="100%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-o3-fastmath-perf-indiv.svg" width="100%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 xml:id="sec-gcc15-spec-compared-to-others">
   <title>Comparison with other compilers</title>

   <para> The SUSE toolchain team regularly uses the SPEC CPU 2017 suite to compare the optimization
    capabilities of GCC with other compilers, primarily LLVM/Clang and ICX from Intel. In the final
    section of this case study, we discuss how the default compiler on SUSE Linux Enterprise Server
    16.0 compares to these competitors. Before starting, note that individuals with significantly
    more expertise in GCC than in the other compilers conducted the comparison and are not
    completely <quote>unbiased</quote>. In addition, keep in mind that all previous explanations
    about how we carry out measurements and patch benchmarks also apply to this section. However,
    because the results inform our own work, rest assured that we strive for accuracy.</para>

   <para> We built the <literal>clang</literal>, <literal>clang++</literal> and
     <literal>flang-new</literal> compilers from sources obtained from the official git repository
    (tag <literal>llvmorg-22.1.7</literal>). We used them to compile the SPEC CPU 2017 suite with
     <literal>-&#8288;Ofast</literal> and <literal>-&#8288;march=native</literal>. We then
    compared the performance against the suites built with GCC 15.2 using the same options. When
    using LLVM/Clang's LTO to compile SPEC, we selected the <emphasis role="italic">full</emphasis>
    variant. This variant provides stronger optimization capabilities, although it is not suitable
    for building large projects.</para>

   <figure xml:id="fig-gcc15-specint-ofast-vsllvm-geomean">
    <title>Overall performance (bigger is better) of C/C++ integer benchmarks built with LLVM/Clang
     22 and GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vsllvm-geomean.svg" width="100%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vsllvm-geomean.svg" width="100%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para><xref linkend="fig-gcc15-specint-ofast-vsllvm-geomean" xrefstyle="template:Figure %n"/>
    shows that the geometric mean of the whole SPEC INTrate 2017 suite runs faster when benchmarks
    are compiled with GCC. <xref linkend="fig-gcc15-specint-ofast-vsllvm-indiv"
     xrefstyle="template:Figure %n"/> which shows results for individual benchmarks confirms this.
    LLVM/Clang produces faster code for two benchmarks: <literal>525.x264_r</literal> by 4% and
     <literal>541.leela_r</literal> by 6% with LTO. However, code generated by GCC performs better
    in more cases and by larger margins. For example, <literal>505.mcf_r</literal> is 11% faster and
     <literal>523.xalancbmk_r</literal> is 17% faster.</para>


   <figure xml:id="fig-gcc15-specint-ofast-vsllvm-indiv">
    <title>Runtime performance (bigger is better) of C/C++ integer benchmarks built with LLVM/Clang
     22 and GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vsllvm-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vsllvm-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>


   <para> The comparison of the geometric mean scores for the SPEC FPrate 2017 suite built with both
    compiler suites is shown in <xref linkend="fig-gcc15-specfp-ofast-vsllvm-geomean"
     xrefstyle="template:figure %n"/>. Individual results are compared in <xref
     linkend="fig-gcc15-specfp-ofast-vsllvm-indiv" xrefstyle="template:figure %n"/>. Again,
    LLVM/Clang outperforms GCC in two benchmarks: <literal>508.namd_r</literal> by 11% and
     <literal>544.nab_r</literal> by 20% when comparing LTO scores. However, GCC generates notably
    faster code in nine cases, sometimes also by a wide margins. The two largest differences are
     <literal>519.lbm_r</literal> at 25% and <literal>538.imagick_r</literal> at 45%. Despite the
    code placement issue we described earlier, <literal>519.lbm_r</literal> compiled with GCC 15 and
    LTO is still a lot faster than when compiled with LLVM/Clang. </para>

   <figure xml:id="fig-gcc15-specfp-ofast-vsllvm-geomean">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 built with LLVM/Clang 22 and
     GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vsllvm-geomean.svg" width="100%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vsllvm-geomean.svg" width="100%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specfp-ofast-vsllvm-indiv">
    <title>Runtime performance (bigger is better) of floating point benchmarks built with LLVM/Clang
     22 and GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vsllvm-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vsllvm-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <!-- ICX -->

   <para>Although Intel compilers are not designed for AMD processors, they are well-known for their
    high-level optimization capabilities, particularly in vectorization. Therefore, we traditionally
    included ICC in our comparisons of compilers. When Intel ddiscontinued this compiler and
    transitioned to ICX, a new compiler built on top of LLVM, we began comparing GCC with ICX
    instead.</para>

   <figure xml:id="fig-gcc15-specint-ofast-vsicx-geomean">
    <title>Overall performance (bigger is better) of SPEC INTrate 2017 built with ICX 2026.0.0 and
     GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vsicx-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vsicx-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>
    <xref linkend="fig-gcc15-specint-ofast-vsicx-geomean" xrefstyle="template:Figure %n"/> shows the
    overall SPEC INTrate scores achived by both compilers using <literal>-&#8288;Ofast</literal>
    and <literal>-&#8288;march=native</literal>. The results of individual benchmarks are in
     <xref linkend="fig-gcc15-specint-ofast-vsicx-indiv" xrefstyle="template:figure %n"/>. Both
    figures show comparable performance between the compilers. The two notable differences are that,
    with LTO, GCC produces <literal>525.x264_r</literal> that is 15% slower and
     <literal>548.exchange2_r</literal> that is 15% faster.</para>

   <figure xml:id="fig-gcc15-specint-ofast-vsicx-indiv">
    <title>Runtime performance (bigger is better) of individual integer benchmarks built with ICX
     2026.0.0 and GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specint-ofast-vsicx-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specint-ofast-vsicx-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <para>When building the SPEC FPrate suite, GCC achieves a 12% higher geometric mean without LTO
    and 5% with LTO (see <xref linkend="fig-gcc15-specfp-ofast-vsicx-geomean"
     xrefstyle="template:figure %n"/>). However, individual benchmark results reveal a more nuanced
    overall picture (see <xref linkend="fig-gcc15-specfp-ofast-vsicx-indiv"
     xrefstyle="template:figure %n"/>). There are benchmarks where GCC generates much faster code
    (most prominently for example in <literal>538.imagick_r</literal> and
     <literal>554.roms_r</literal>). However, there are also benchmarks where ICX generates
    considerably faster code (especially in <literal>519.lbm_r</literal> and
     <literal>544.nab_r</literal>, and non-LTO results suggest ICX would remain faster even without
    the code placement issue in GCC described earlier). </para>

   <para> Nevertheless, in conclusion GCC performs consistently and competitively against these
    high-performance compilers.</para>

   <figure xml:id="fig-gcc15-specfp-ofast-vsicx-geomean">
    <title>Overall performance (bigger is better) of SPEC FPrate 2017 built with ICX 2026.0.0 and
     GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vsicx-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vsicx-geomean.svg" width="85%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

   <figure xml:id="fig-gcc15-specfp-ofast-vsicx-indiv">
    <title>Runtime performance (bigger is better) of individual floating point benchmarks built with
     ICX 2026.0.1 and GCC 15.2</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="gcc15-specfp-ofast-vsicx-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="gcc15-specfp-ofast-vsicx-indiv.svg" width="90%" format="SVG"/>
     </imageobject>
    </mediaobject>
   </figure>

  </sect2>
 </sect1>


 <?pdfpagebreak style="sbp" formatter="fop"?>

 <xi:include href="sbp-legal-notice.xml"/>


 <?pdfpagebreak style="sbp" formatter="fop"?>
 <xi:include href="license-gfdl.xml"/>
</article>
