.Test {testname}
==========
.{testComp}
- {testcomponent}

.{testDescr}
- {testdescription}

.{testProc}
. {testprocedure}
+
[subs="attributes,quotes"]
----
{testcommand}
----

.{testRecover}
. Clean up the sbd device (or devices) - to be called on any cluster node
+
[subs="attributes,quotes"]
----
# cs_clear_sbd_devices --all
----
. Start the cluster on failed node - to be called on any cluster node
+
[subs="attributes,quotes"]
----
# crm cluster start --all
----

.{testExpect}
. The cluster detects missing {nodefail}
. The {nodefail} gets fenced.
. After some time, the cluster shows the `srHook` attribute on the missing
secondary {sitefail} ({nodefail}) as `SFAIL`.
. The failed {nodefail} does not join the cluster - *admin to process the recovery procedure*
. Once {nodefail} joins back to the cluster, {HANA} is started by the cluster.
. After the some time, the new secondary is marked as in sync (SOK).
. The cluster "failed actions" are cleaned up after following the recovery procedure.
==========

